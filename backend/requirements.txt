fastapi==0.111.0
starlette>=0.37.2,<0.38.0
pydantic>=2.7,<3.0
uvicorn[standard]>=0.30,<1.0
pytest>=8.0,<9.0
httpx>=0.27,<0.29
sqlmodel>=0.0.18
chromadb>=0.5.0
google-generativeai>=0.5.0
instructor>=1.2.6
python-dotenv>=1.0.0
pgvector>=0.2.0
psycopg2-binary>=2.9.0
# PageIndex: vectorless reasoning-based RAG for policy-doc citation anchoring.
# Phase 1 ships the regex fallback only; Phase 2 wires the live invocation when
# POLICY_PARSER=pageindex and OPENAI_API_KEY are set. Pin to a known-good tag —
# upstream is moving quickly, and the dispatcher catches ImportError so older
# pins keep working.
pageindex==0.1.4
# Defense-package PDF rendering. reportlab imports PIL at load, so pin pillow too.
reportlab>=4.0.0
pillow>=10.0.0
# Password hashing (salted, slow) — replaces legacy unsalted sha256.
bcrypt>=4.1,<6.0
# S3-compatible object storage (STORAGE_BACKEND=s3): AWS S3, Cloudflare R2,
# Backblaze B2, etc. Only used when the S3 backend is selected.
boto3>=1.34,<2.0
# MCP client SDK for the real comms source adapter (COMMS_MCP_*_SSE_URL). Imported
# lazily in app/ingestion/comms/mcp_source.py — only needed when a real MCP source
# is configured; the simulated sources and tests don't require it.
mcp>=1.0,<2.0
# xlsx loss-run parsing (app/extraction/readers.py). Imported lazily — only the
# xlsx reader path needs it; CSV extraction is stdlib-only.
openpyxl>=3.1
