"""Venue operational-data ingestion spine.

A reusable connector framework that pulls venue operational signals into a
normalized event store and rolls them up into the risk score, so that
ingesting data visibly moves a venue's score/tier.
"""
